// The lead is the landing core's; what this brand asks of one is `LEAD` in the
// site config. The slice keeps its public names so the funnel reads one place.
export type { Lead, LeadCandidate, SpamVerdict } from "@/shared/landing/core/lead";
export { validateLead } from "@/shared/config/lead";
